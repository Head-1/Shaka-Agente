# Registro de retomada — 25 de agosto de 2026

## Objetivo

Este registro marca a retomada controlada do desenvolvimento do Shaka-Agente a partir da release `v0.8.2`. Não altera a versão do produto, não declara uma nova release e não substitui o `README.md`, o `RUNBOOK_OPERACIONAL.md` ou os documentos normativos de segurança.

## Ambiente validado

| Item | Resultado |
|---|---|
| Repositório | `Head-1/Shaka-Agente` |
| Branch de retomada | `chore/resume-project` |
| Commit-base | `e826d0d` — merge da atualização da API pública |
| Versão do workspace | `0.8.2` |
| Rust/Cargo usados | `1.98.0` |
| Rust mínimo declarado | `1.85` |
| Edição Rust | `2024` |
| Dependências | `Cargo.lock` usado com `--locked` |

## Validação executada

A formatação passou com `cargo fmt --all -- --check`. A suíte `cargo test --workspace --locked` passou com **85 testes aprovados, 0 falhas e 0 ignorados**. O gate `cargo clippy --workspace --all-targets --locked -- -D warnings -A missing_docs -A clippy::missing_errors_doc` terminou sem erros.

A auditoria de dependências com `cargo audit` na versão `0.22.2`, alinhada ao CI, terminou sem advisories reportados. Os fluxos locais de continuidade também foram executados em diretório temporário: `config`, `doctor`, `sandbox-demo`, execução em `dry-run` e `verify-audit` com papel administrador. O `doctor` reportou configuração válida, integridade do banco válida e status `ready`; o `verify-audit` confirmou a cadeia com um evento verificado; o sandbox retornou `exit_code: 42` e `fuel_consumed: 2`; a execução em dry-run terminou com sucesso sem resultados de ferramenta externa.

A tentativa de executar `verify-audit` como operador foi bloqueada por autorização insuficiente, conforme esperado pelo RBAC. Esse bloqueio é resultado correto do modelo de segurança, não uma falha do sistema.

## Pendências conhecidas

O Item 2, referente ao guard de deduplicação de `force_unknown`, permanece pendente de validação formal. O handoff existente registra que ainda não foi coletada a prova obrigatória de falha na asserção correta antes do patch, seguida de sucesso após o patch e da suíte completa pós-patch. Este registro não marca o Item 2 como concluído e nenhuma alteração do Item 2 foi incluída nesta retomada.

Os achados de configuração local, higiene de repositório, integridade de destino no restore, idempotência de step, SBOM e recomendações de Rust permanecem no backlog. Eles não foram alterados nesta retomada porque exigem escopo e validação próprios.

## Governança e versionamento

A retomada ocorre em branch dedicada, sem alteração direta na `main`, sem merge automático e sem bump de versão. O commit desta retomada deve ser assinado com a chave SSH de assinatura cadastrada no GitHub para este ambiente. O segundo ambiente deve usar uma chave de assinatura própria, cadastrada separadamente, mantendo a mesma política Git e o mesmo contrato de validação.

> Estado operacional: a base `v0.8.2` está compilável, testada e adequada para a próxima etapa de desenvolvimento controlado; isso não equivale a autorização para exposição pública, efeitos externos ou operação multi-tenant distribuída.

## Próxima etapa

Qualquer correção de código deverá seguir branch dedicada, teste de regressão que falhe no código anterior, correção mínima, suíte completa, revisão do diff e aprovação humana antes de PR ou merge.
